---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: cloud9-fedora-rawhide
    image: fedora:rawhide
    registry:
      url: registry.fedoraproject.org
provisioner:
  name: ansible
  inventory:
    group_vars:
      all:
        cloud9_ignore_docker: no
  lint:
    name: ansible-lint
scenario:
  name: fedora-rawhide
verifier:
  name: goss
  lint:
    name: yamllint
